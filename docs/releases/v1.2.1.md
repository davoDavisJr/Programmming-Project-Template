# v1.2.1 Patch Update

This patch update standardizes VS Code PDF preview behavior on the base `main`
template and records the release in the repository documentation.

## Changed

- Added the LaTeX Workshop PDF viewer association for `*.pdf` files on `main`.
- Recommended LaTeX Workshop where PDF association is configured.
- Added this release note for the patch record.

## Scope

The course/profile branches remain at their previous release tips. The earlier
cross-branch publication attempt was reverted so those branches are not opened
as merge candidates against `main`.
