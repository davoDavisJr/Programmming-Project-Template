# ENG1013 Python Profile

This branch is a Python-first profile built from the clean Monash base
template.

It requires Python `3.10.11` and intentionally avoids package restrictions until
specific coursework requirements are known.

Use `src/main.py` as the initial entrypoint, then add more files under `src/`
when a task grows.
