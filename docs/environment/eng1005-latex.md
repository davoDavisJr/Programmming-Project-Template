# ENG1005 LaTeX Profile

This branch is a LaTeX-first profile built from the clean Monash base template.

Use `LaTeX/main.tex` as the report entrypoint and keep generated output under
`build/latex/` or `docs/export/`, both of which are ignored by Git.
